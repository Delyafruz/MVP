# Объединённая система управления игроком

Система Player в проекте MVP объединяет два набора скриптов:
- **PlayerMovement + PlayerStateManager** (оригинальные скрипты MVP)
- **PlayerController + PlayerInteractions + PlayerCursor** (система из Cable-physics)

## ✅ Что было сделано

### 1. Интеграция PlayerController с PlayerStateManager

**PlayerController** теперь автоматически интегрируется с `PlayerStateManager`:

```csharp
// При состоянии TerminalFocus:
- FreezCamera = true
- FreezMovement = true
- Курсор разблокирован и виден

// При состоянии Roaming:
- FreezCamera = false
- FreezMovement = false
- Курсор заблокирован и скрыт
```

**Изменения в коде:**
- Добавлена подписка на `PlayerStateManager.Instance.OnStateChanged`
- Метод `OnPlayerStateChanged()` управляет FreezCamera и FreezMovement
- Автоматическая отписка в `OnDestroy()`

### 2. Интеграция PlayerInteractions с PlayerStateManager

**PlayerInteractions** теперь блокирует взаимодействие при работе с терминалом:

```csharp
// При состоянии TerminalFocus:
- canInteract = false
- Все удерживаемые объекты автоматически бросаются
- Выделение объектов снимается
- Ввод ЛКМ игнорируется

// При состоянии Roaming:
- canInteract = true
- Можно взаимодействовать с объектами
```

**Изменения в коде:**
- Добавлен флаг `canInteract`
- Проверки состояния в `UpdateInput()` и `UpdateSelectedObject()`
- Автоматическийброс объектов при переходе в TerminalFocus

### 3. PlayerMovement помечен как устаревший

Старый **PlayerMovement.cs** сохранён для обратной совместимости, но помечен как `DEPRECATED`:

```csharp
// ============================================================================
// УСТАРЕЛО (DEPRECATED)
// Рекомендуется использовать PlayerController из SamplePlayerController
// ============================================================================
```

**Почему PlayerController лучше:**
- ✅ Управление FPS камерой мышью
- ✅ Плавное ускорение движения (SmoothDamp)
- ✅ Несколько режимов (Default/Sprint)
- ✅ Интеграция с PlayerStateManager
- ✅ Поддержка взаимодействия с кабелями
- ✅ События (OnPlayerEnterPortal)

## 📋 Структура системы

```
Player (GameObject)
├── CharacterController          ← Физика движения
├── PlayerStateManager           ← Управление состояниями (Singleton)
├── PlayerController             ← FPS движение + камера + интеграция с состояниями
├── PlayerInteractions           ← Взаимодействие с объектами + интеграция с состояниями
└── PlayerCursor (опционально)   ← UI курсор при наведении

Дочерние объекты:
├── Camera (Transform + Camera)  ← Камера игрока
└── HandTransform               ← Позиция удержания объектов
```

## 🎮 Состояния игрока (PlayerState)

### Roaming (Свободное перемещение)

**PlayerController:**
- ✅ Движение WASD
- ✅ Мышь управляет камерой
- ✅ Shift - бег
- ✅ Space - прыжок
- ✅ Курсор заблокирован

**PlayerInteractions:**
- ✅ ЛКМ - поднять/бросить объект
- ✅ Выделение объектов при наведении
- ✅ UI курсор виден

### TerminalFocus (Работа с терминалом)

**PlayerController:**
- ❌ Движение заблокировано
- ❌ Камера заблокирована
- ✅ Курсор разблокирован и виден

**PlayerInteractions:**
- ❌ Взаимодействие с объектами заблокировано
- ❌ Выделение объектов отключено
- ✅ Удерживаемые объекты автоматически бросаются

## 🔧 Настройка

### Вариант 1: PlayerController (рекомендуется)

```
1. Создать GameObject "Player"
2. Добавить компоненты:
   - CharacterController
   - PlayerStateManager
   - PlayerController (из SamplePlayerController/)
   - PlayerInteractions (из SamplePlayerController/)
   - PlayerCursor (опционально)

3. Создать дочерние объекты:
   - Camera (позиция: 0, 1.6, 0) с Camera component
   - HandTransform (позиция: 0.3, 1.4, 0.5)

4. Настроить PlayerController:
   - Player Camera → Camera
   - Ground Mask → слой земли
   - Mouse Sensitivity → по вкусу (по умолчанию 8)

5. Настроить PlayerInteractions:
   - Player Camera → Camera Transform
   - Hand Transform → HandTransform
   - Select Layer → "Interactable"
   - Held Object Layer → "HeldObject"

6. Настроить слои в Project Settings → Tags and Layers
```

### Вариант 2: PlayerMovement (устарело, для простых проектов)

```
1. Создать GameObject "Player"
2. Добавить компоненты:
   - CharacterController
   - PlayerStateManager
   - PlayerMovement (из Assets/Scripts/)

3. Создать дочерний объект GroundCheck (позиция: 0, 0, 0)

4. Настроить PlayerMovement:
   - Ground Check → GroundCheck Transform
   - Ground Mask → слой земли
```

**Ограничения PlayerMovement:**
- ❌ Нет управления камерой мышью
- ❌ Нет взаимодействия с объектами
- ❌ Нет поддержки кабелей

## 🔀 Переключение состояний

Для переключения между состояниями используйте:

```csharp
// В любом скрипте:
using UnityEngine;

public class Terminal : MonoBehaviour
{
    void StartWork()
    {
        // Переключаемся на работу с терминалом
        PlayerStateManager.Instance.SetState(PlayerState.TerminalFocus);
    }

    void StopWork()
    {
        // Возвращаемся к свободному перемещению
        PlayerStateManager.Instance.SetState(PlayerState.Roaming);
    }
}
```

**Все остальное происходит автоматически:**
- PlayerController блокирует/разблокирует управление
- PlayerInteractions блокирует/разблокирует взаимодействие
- Курсор автоматически показывается/скрывается

## 🧩 Совместимость компонентов

| Компонент | Требует | Совместим с |
|-----------|---------|-------------|
| PlayerStateManager | - | Все (Singleton) |
| PlayerController | CharacterController | PlayerStateManager |
| PlayerInteractions | PlayerController | PlayerStateManager |
| PlayerCursor | PlayerInteractions | - |
| PlayerMovement (deprecated) | CharacterController | PlayerStateManager |

## 📝 Примеры использования

### Пример 1: Терминал с блокировкой управления

```csharp
using UnityEngine;

public class TerminalController : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Player"))
        {
            // Игрок подошёл к терминалу, переключаем состояние
            PlayerStateManager.Instance.SetState(PlayerState.TerminalFocus);
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Player"))
        {
            // Игрок отошёл от терминала, возвращаем управление
            PlayerStateManager.Instance.SetState(PlayerState.Roaming);
        }
    }
}
```

### Пример 2: Работа с кабелями

```csharp
using HPhysic;
using HInteractions;

// Кабель автоматически поднимается через PlayerInteractions
// Коннекторы автоматически подключаются при броске (PhysicCableCon)
// Система полностью интегрирована!
```

### Пример 3: Проверка текущего состояния

```csharp
using UnityEngine;

public class Example : MonoBehaviour
{
    void Update()
    {
        if (PlayerStateManager.Instance.CurrentState == PlayerState.TerminalFocus)
        {
            // Игрок работает с терминалом
            Debug.Log("Working on terminal");
        }
        else
        {
            // Игрок свободно перемещается
            Debug.Log("Roaming");
        }
    }
}
```

## 🐛 Решение проблем

### Игрок не двигается

**Проверьте:**
1. CharacterController добавлен?
2. PlayerStateManager в состоянии Roaming?
3. FreezMovement = false в PlayerController?

### Не работает взаимодействие с объектами

**Проверьте:**
1. PlayerInteractions добавлен?
2. Объекты на слое Select Layer?
3. Объекты имеют компонент Interactable/Liftable?
4. PlayerStateManager в состоянии Roaming?

### Камера не двигается

**Проверьте:**
1. Player Camera назначена в PlayerController?
2. FreezCamera = false?
3. PlayerStateManager в состоянии Roaming?

### Курсор не скрывается

Это нормально при состоянии TerminalFocus. Если курсор виден при Roaming - проверьте, что PlayerController правильно интегрирован с PlayerStateManager.

## 🎯 Рекомендации

✅ **Используйте PlayerController** для новых проектов  
✅ **Используйте PlayerStateManager** для управления состояниями  
✅ **Добавьте PlayerInteractions** для работы с кабелями  
⚠️ **PlayerMovement** оставьте только для обратной совместимости  

Система полностью готова к работе! 🚀
